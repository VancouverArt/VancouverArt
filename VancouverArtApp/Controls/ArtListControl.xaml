<UserControl
    x:Class="VancouverArtApp.Controls.ArtListControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VancouverArtApp.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <UserControl.Resources>
        <DataTemplate x:Key="DataTemplateArtItem">
            <Grid Height="26" Width="203">
                <TextBlock x:Name="textBlock" HorizontalAlignment="Left" Margin="0,0,0,6" TextWrapping="Wrap" Text="{Binding Title}" d:LayoutOverrides="Height"/>
            </Grid>
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <ListView Grid.Column="1" ItemsSource="{Binding Art}" ItemTemplate="{StaticResource DataTemplateArtItem}"/>
    </Grid>
</UserControl>
